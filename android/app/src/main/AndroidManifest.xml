<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Required by geolocator for the profile screen's "Auto Fill" city
         button; requesting it is never blocking — see EditProfileScreen. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Required on Android 13+ for NotificationService to be allowed to
         show reminder notifications at all; the runtime prompt happens via
         AndroidFlutterLocalNotificationsPlugin.requestNotificationsPermission(). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- The daily/weekly/timetable reminders are scheduled with
         flutter_local_notifications' exact-alarm mode so they fire at the
         precise minute rather than being batched by the OS. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="CTET TET Prep"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Real AdMob App ID (admob.google.com, app "CTET & State TET
             Prep") — see lib/core/services/ad_service.dart for the
             matching real ad unit IDs. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4806343972940588~1328644052"/>
        <!-- Without this, FCM falls back to its own auto-created channel
             (default importance) whenever the app is backgrounded/killed
             when a push arrives, which sits silently in the notification
             shade instead of popping up as a heads-up banner. Pointing it
             at NotificationService's own channel (created with
             Importance.high in notification_service.dart) makes
             admin-sent pushes pop up the same way foreground ones do. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel"/>
        <!-- Without this, every zonedSchedule() alarm from NotificationService
             gets silently wiped by Android on every device reboot (this is
             normal OS behavior for AlarmManager alarms) and never comes back
             until the student happens to reopen the app — which re-runs
             initialize() and re-schedules everything. The
             RECEIVE_BOOT_COMPLETED permission above does nothing on its own;
             this receiver is what flutter_local_notifications actually uses
             to re-arm pending notifications right after boot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
